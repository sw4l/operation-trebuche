// Note: this model config is created with 4-space [tab] symbols. If you use different settings, the structure may not be as clear.
class CfgSkeletons
{
	class Default
	{
		isDiscrete = 1;
		skeletonInherit = "";
		skeletonBones[] = {};
	};

	// Skeleton of undamaged variant of the house, containing doors and windows. Note that glass #3 is not a window but the glass pane of glass door (door #1). Therefore
	// (similar to door handles) it's bones need to be appropriately linked to bones of the door so that it moves along with it when the door is opened/closed.
	class highway_gate_a_skeleton: Default
	{
		skeletonInherit = "Default";
		skeletonBones[] =
		{
			// Pairs: 1st is newly defined bone, second is it`s "parent" in the skeleton (bone hierarchy).
			"Door_l",				"",
			"Door_l_lock",				"Door_l",
			"Door_r",				"",
			"Door_r_lock",				"Door_r"
		};
	};
	class Gate_house_skeleton: Default
	{
		skeletonInherit = "Default";
		skeletonBones[] =
		{
			// Pairs: 1st is newly defined bone, second is it`s "parent" in the skeleton (bone hierarchy).
			"door_1_a",				"",
			"door_1_b",				"",
			"door_2_a",				"",
			"door_2_b",				"",
			"door_3_a",				"",
			"door_3_b",				"",
			"door_4_a",				"",
			"door_4_b",				"",
			"door_5_a",				"",
			"door_5_b",				"",
			"lift",					"",
			"gate",					""
		};
	};
};

class CfgModels
{
	class highway_gate_a // class name must be the same as name of p3d
	{
		skeletonName = "highway_gate_a_skeleton"; // Set to use above defined skeleton.
		sections[] =
		{
			// Selections from the model that are used in config.cpp for switching textures and materials.
		};
		sectionsInherit = "";		
		class Animations
		{
			// Animation for door #1
			class Door_1_rot
			{
				type			= translation;
				source			= gate_door_1; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_l; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= motionaxis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				offset0=0; 
				offset1=-.47; 
			};

			// Animation for door #2
			class Door_2_rot: Door_1_rot
			{
				source			= gate_door_2;
				selection		= door_r;
				axis			= motionaxis;
				minValue		= 0.1; // Rotation of door starts at 0.1 instead of 0, because from 0 to 0.1 the handle is pressed down.
				offset0=0; 
				offset1=.58; 
			};
			class lock_1_rot
			{
				type			= translation;
				source			= gate_door_1; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_l_lock; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= motionaxis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				offset0=0; 
				offset1=-.1; 
			};
			class lock_2_rot: lock_1_rot
			{
				source			= gate_door_2;
				selection		= door_r_lock;
				axis			= motionaxis;
				minValue		= 0.1; // Rotation of door starts at 0.1 instead of 0, because from 0 to 0.1 the handle is pressed down.
				offset0=0; 
				offset1=.1; 
			};
		};
	};
	class highway_gate_b // class name must be the same as name of p3d
	{
		skeletonName = "highway_gate_a_skeleton"; // Set to use above defined skeleton.
		sections[] =
		{
			// Selections from the model that are used in config.cpp for switching textures and materials.
		};
		sectionsInherit = "";		
		class Animations
		{
			// Animation for door #1
			class Door_1_rot
			{
				type			= translation;
				source			= gate_door_1; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_l; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= motionaxis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				offset0=0; 
				offset1=-.47; 
			};

			// Animation for door #2
			class Door_2_rot: Door_1_rot
			{
				source			= gate_door_2;
				selection		= door_r;
				axis			= motionaxis;
				minValue		= 0.1; // Rotation of door starts at 0.1 instead of 0, because from 0 to 0.1 the handle is pressed down.
				offset0=0; 
				offset1=.58; 
			};
			class lock_1_rot
			{
				type			= translation;
				source			= gate_door_1; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_l_lock; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= motionaxis; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				offset0=0; 
				offset1=-.1; 
			};
			class lock_2_rot: lock_1_rot
			{
				source			= gate_door_2;
				selection		= door_r_lock;
				axis			= motionaxis;
				minValue		= 0.1; // Rotation of door starts at 0.1 instead of 0, because from 0 to 0.1 the handle is pressed down.
				offset0=0; 
				offset1=.1; 
			};
		};
	};
	class Gate_house // class name must be the same as name of p3d
	{
		skeletonName = "Gate_house_skeleton"; // Set to use above defined skeleton.
		sections[] =
		{
			// Selections from the model that are used in config.cpp for switching textures and materials.
		};
		sectionsInherit = "";		
		class Animations
		{
			// Animation for door #1aa
			class Door_1_a_rot
			{
				type			= translation;
				source			= source_door_1; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_1_a; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= motionaxis_1; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				offset0=0; 
				offset1=.44; 
			};
			class Door_1_b_rot: Door_1_a_rot
			{
				source			= source_door_1;
				selection		= door_1_b;
				axis			= motionaxis_1;
				minValue		= 0.1; // Rotation of door starts at 0.1 instead of 0, because from 0 to 0.1 the handle is pressed down.
				offset0=0; 
				offset1=-.44; 
			};
			class Door_2_a_rot
			{
				type			= translation;
				source			= source_door_2; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_2_a; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= motionaxis_2; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				offset0=0; 
				offset1=.44; 
			};
			class Door_2_b_rot: Door_2_a_rot
			{
				source			= source_door_2;
				selection		= door_2_b;
				axis			= motionaxis_2;
				minValue		= 0.1; // Rotation of door starts at 0.1 instead of 0, because from 0 to 0.1 the handle is pressed down.
				offset0=0; 
				offset1=-.44; 
			};
			class Door_3_a_rot
			{
				type			= translation;
				source			= source_door_3; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_3_a; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= motionaxis_3; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				offset0=0; 
				offset1=.44; 
			};
			class Door_3_b_rot: Door_3_a_rot
			{
				source			= source_door_3;
				selection		= door_3_b;
				axis			= motionaxis_3;
				minValue		= 0.1; // Rotation of door starts at 0.1 instead of 0, because from 0 to 0.1 the handle is pressed down.
				offset0=0; 
				offset1=-.44; 
			};
			class Door_4_a_rot
			{
				type			= translation;
				source			= source_door_4; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_4_a; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= motionaxis_4; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				offset0=0; 
				offset1=.44; 
			};
			class Door_4_b_rot: Door_4_a_rot
			{
				source			= source_door_4;
				selection		= door_4_b;
				axis			= motionaxis_4;
				minValue		= 0.1; // Rotation of door starts at 0.1 instead of 0, because from 0 to 0.1 the handle is pressed down.
				offset0=0; 
				offset1=-.44; 
			};
			class Door_5_a_rot
			{
				type			= translation;
				source			= source_door_5; // Controler defined in class AnimationSources in config.cpp.
				selection		= door_5_a; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= motionaxis_5; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				offset0=0; 
				offset1=.44; 
			};
			class Door_5_b_rot: Door_5_a_rot
			{
				source			= source_door_5;
				selection		= door_5_b;
				axis			= motionaxis_5;
				minValue		= 0.1; // Rotation of door starts at 0.1 instead of 0, because from 0 to 0.1 the handle is pressed down.
				offset0=0; 
				offset1=-.44; 
			};
			class lift_rot
			{
				type			= translation;
				source			= source_lift; // Controler defined in class AnimationSources in config.cpp.
				selection		= lift; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= motionaxis_7; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				offset0=0; 
				offset1=-1.11; 
			};
			class gate_rot
			{
				type			= translation;
				source			= source_gate; // Controler defined in class AnimationSources in config.cpp.
				selection		= gate; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
				axis			= motionaxis_6; // Axis of the rotation.
				memory			= 1;
				minValue		= 0; // Value of the controller at which the rotation will be angle0
				maxValue		= 1; // Value of the controller at which the rotation will be angle1
				offset0=0; 
				offset1=0.95; 
			};
		};
	};
};